import styled from '@emotion/styled';

const Wrapper = styled.div`
  padding: 1.3rem 0;
  margin-top: 3rem;
  background: ${(props) => props.theme.color.gray};
  text-align: center;
  color: #949895;
  p {
    margin-bottom: 0;
  }
`;

export {
  Wrapper
};
