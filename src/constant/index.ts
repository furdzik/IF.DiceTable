export enum ButtonsSizes {
  Small,
  Normal,
  Large
}

export enum ButtonTypes {
  Button,
  Submit,
  Reset,
  Link
}

export enum InputTypes {
  Text = 'text',
  File = 'file',
  Email = 'email',
  Number = 'number'
}